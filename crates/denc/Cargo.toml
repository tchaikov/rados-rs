[package]
name = "denc"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.5"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
glob = "0.3"

[[bin]]
name = "dencoder"
path = "src/bin/dencoder.rs"
